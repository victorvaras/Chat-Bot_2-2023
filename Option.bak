#lang racket

(require "Extras.rkt")

;TDA Option
; (list indice, mensaje, chat-bot, flujo, palabras_alternativas, 


;Constructor de option
(define option (lambda (indice mensaje num_chat flujo . args)

        (append (list indice mensaje num_chat flujo)args)

  ))







(define op1 (option  1 "1) Viajar" 2 4 "viajar" "turistear" "conocer") )
(define a1 (option 1 "hola" 1 2 3))